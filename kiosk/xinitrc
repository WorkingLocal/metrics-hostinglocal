#!/bin/bash
# Grafana kiosk X11 sessie — METRICSSERVER HDMI display
# Gestart via startx vanuit ~/.bash_profile (autologin tty1)
# Dashboard rotatie via Grafana Playlist (5s interval, ingebouwde functie)

sleep 1
xrandr --output HDMI-1 --mode 1920x1080 --rate 60 2>/dev/null || \
xrandr --output HDMI-A-1 --mode 1920x1080 --rate 60 2>/dev/null || true

xset s off
xset -dpms
xset s noblank

unclutter -idle 1 -root &
openbox &

sleep 2

CHROME_BIN=""
for candidate in chromium-browser chromium google-chrome-stable google-chrome; do
    if command -v "$candidate" &>/dev/null; then
        CHROME_BIN="$candidate"
        break
    fi
done

if [[ -z "$CHROME_BIN" ]]; then
    echo "FOUT: geen Chromium/Chrome gevonden" | tee /tmp/kiosk-error.log
    exit 1
fi

# Grafana Playlist — roteert automatisch tussen 3 FHD dashboards (5s interval)
# Playlist beheren via: http://metrics.hostinglocal.be/playlists
PLAYLIST_URL="http://metrics.hostinglocal.be/playlists/play/afp1f2l41syrkd?kiosk&theme=dark"

while true; do
    "$CHROME_BIN" \
        --kiosk \
        --noerrdialogs \
        --disable-infobars \
        --no-first-run \
        --disable-translate \
        --disable-extensions \
        --disable-component-update \
        --check-for-update-interval=31536000 \
        --disable-features=TranslateUI \
        --password-store=basic \
        --disable-session-crashed-bubble \
        --disable-restore-session-state \
        --disk-cache-size=1 \
        "$PLAYLIST_URL"
    sleep 5
done
